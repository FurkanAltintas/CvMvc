@model CvMvc.Models.EntityFramework.Note

@{
    ViewBag.Title = "Not Düzenle";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@using (Html.BeginForm("NoteEdit", "Admin", FormMethod.Post))
{
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Not Düzenleme</h3>
            <div class="card-tools">
                <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                    <i class="fas fa-minus"></i>
                </button>
                <button type="button" class="btn btn-tool" data-card-widget="remove" title="Remove">
                    <i class="fas fa-times"></i>
                </button>
            </div>
        </div>
        <div class="card-body" style="display: block;">
            @Html.TextBoxFor(x => x.Message, new { @class = "form-control" })
        </div>
    </div>

    <button type="submit" class="btn btn-dark">Güncelle</button>
}
